# Classes

# Person "class".
Person = (name) ->
({
	name: name,
	introduce: () -> print("Hello, my name is {this.name}!")
})

# Construct a Person instance.
bob = Person("Bob")

# Call an instance method.
bob.introduce()
