# Turn Text Uppercase

def char_upper(char)
{
  if ord(char) < ord("a") || ord(char) > ord("z")
    return char
  return chr(ord(char)+ord("A")-ord("a"))
}

def upper(string)
{
  out = ""
  for i from 0 till len(string)
    out = out + char_upper(string[i])
  return out
}

print(upper("abcdefghijklmnopqrstuvwxyz012356789"))