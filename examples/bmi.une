# BMI Calculator

bmi = (height, weight) -> weight / height ** 2

is_overweight = bmi -> (
	bmi < 25 ? 		"Not overweight" :
	bmi == 25 ? 	"Barely, but not overweight" :
					"Overweight"
)

print(is_overweight(bmi(1.80, 70)))
