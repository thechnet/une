# BMI Calculator

bmi = function(height, weight)
{
  return weight/height**2
}

is_overweight = function(bmi)
{
  if bmi < 25
    return "Not overweight."
  elif bmi == 25
    return "Barely, but not overweight."
  else
    return "Overweight."
}

print(is_overweight(bmi(1.80, 70)))
